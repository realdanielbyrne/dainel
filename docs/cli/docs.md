---
summary: "CLI reference for `dainel docs` (search the live docs index)"
read_when:
  - You want to search the live Dainel docs from the terminal
---

# `dainel docs`

Search the live docs index.

```bash
dainel docs browser extension
dainel docs sandbox allowHostControl
```

